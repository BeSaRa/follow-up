import { Route } from '@angular/router'

/**
 * Production stub for {@link devOnlyRoutes}. Excludes the showcase (and any
 * other dev-only routes) from production builds. Swapped in via the build's
 * `fileReplacements` configuration.
 */
export const devOnlyRoutes: Route[] = []
