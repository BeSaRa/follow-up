import { ProvideUrlService } from './provide-url-service';

describe('ProvideUrlService', () => {
  it('should create an instance', () => {
    expect(new ProvideUrlService()).toBeTruthy();
  });
});
